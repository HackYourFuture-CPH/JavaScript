// Console.log
// prints out what you want to print
// Prints on the console.

// var vs let
var x = 5; 
console.log(x);
var x = 'hello';
console.log(x);

/**
 * 
Values are the "things" that you assign to a variable.
All values have a type. In our example above, the variable x is assigned a value of type number. JavaScript supports the following types:
* string, e.g. "HackYourFuture", "hello"
* number, e.g. 5, or 10.6
* boolean, e.g. true or false
* array*, e.g. [1, 2, 3] or ['what', 'is', 'your', 'name']
* object, e.g. {name: 'John', age: 24}, or the special object null
* function, e.g. function () { return 4; }
 *
 * 
 */


// assignment operator =
// comparison operator ==
// comparision operator === that compares types and values
let a = 12;

console.log(12 === '12');


