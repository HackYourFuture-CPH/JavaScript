let isRaining = true;

// if not isRaining
/**
 * arr[3] = arr of 3  OR arr subscript 3
 * ! = NOT
 * && = AND AND
 * || = OR OR 
 */
if (!isRaining) {
    console.log("I will go out");
} else {
    console.log("I will sit on my couch");
}