// first name and last name constants
// compute (crate) a new one called name

// constant age
// compute (calculate) birth year (approx)

const firstName = 'rohit'
const lastName = 'sharma'

const fullName = firstName + ' ' + lastName

console.log(fullName)

const age = 50
const yearNow = 2022
const birthYear = yearNow - age

console.log(birthYear)
