const arr = [1, 2, "a", "hello", false, 8, 9.12];
console.log(arr);
// length is the property of array
// it gives you the number of elements in that array
console.log(arr.length);
console.log(arr.indexOf(2));
// arr of 3 / arr subscript 3
// [] : subscript operator
// used to access the elements of arrays by index
console.log(arr[3]);